import React from 'react';
import Projects from '../Projects/Projects';

const home = (props) => {
    return (
        <React.Fragment>
            <Projects />
        </React.Fragment>
    );
}

export default home;